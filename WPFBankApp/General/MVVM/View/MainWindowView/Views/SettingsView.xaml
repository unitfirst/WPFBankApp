<UserControl x:Class="WPFBankApp.General.MVVM.View.MainWindowView.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             mc:Ignorable="d"
             d:DesignHeight="450" 
             d:DesignWidth="800"
             Background="{StaticResource Color.Main.Onyx.Dark}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40"/>
            <RowDefinition Height="60"/>
            <RowDefinition/>
            <RowDefinition Height="40"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="1" 
                   Style="{StaticResource Title.Style}"
                   Text="Settings"/>
        
        <ComboBox x:Name="UserList" 
                  Grid.Row="2" 
                  Margin="40 0"
                  Width="200"
                  HorizontalAlignment="Left"
                  Height="30" 
                  VerticalAlignment="Top"
                  VerticalContentAlignment="Center">
            <TextBlock>Consultant</TextBlock>
            <TextBlock>Manager</TextBlock>
        </ComboBox>
    </Grid>
</UserControl>
